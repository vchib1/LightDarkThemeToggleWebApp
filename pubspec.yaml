name: light_dark_theme_toggle_web
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  light_dark_theme_toggle: ^1.0.1
  shared_preferences: ^2.3.5
  provider: ^6.1.2
  flutter_svg: ^2.0.17
  universal_code_viewer: ^1.0.1+3
  url_launcher: ^6.3.1
  flutter_animate: ^4.5.2
  dart_code_viewer2: ^1.0.2
  flutter_highlight: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
     - assets/

